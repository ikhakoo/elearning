require 'test_helper'

class CourseTest < ActiveSupport::TestCase
  # test "shouldn't create course if price is negative" do
  #     assert_no_difference('Course.count') do
  #       begin
  #         create(:invalid_course)
  #       rescue => ex
  #         assert_match("Validation failed: Price must be greater than or equal to 0", ex.message)
  #       end
  #     end
  # end

end
