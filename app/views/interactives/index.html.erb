<h1>Currently <%= @interactives.length %> public sessions</h1>

<% @interactives.each do |interactive| %>
  <%= interactive.about %>
  <%= interactive.url %>
<p><%= link_to "Edit", edit_interactive_path(interactive) %> | <%= link_to "Delete", (interactive), method: :delete, data: {confirm: "Are you sure you want to delete this session?"}  %></p> </div>
<% end %>	