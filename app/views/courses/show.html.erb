<p id="notice"><%= notice %></p>
<div style="text-align:center;">
<p>
  <strong>Name:</strong>
  <%= @course.name %>
</p>
<p>
  <strong>Price:</strong>
  <%= @course.price %>
</p>
<p>
  <strong>Description:</strong>
  <%= @course.description %>
</p>

<!-- Admin has full access to editing function -->
<!-- Instructor can edit course title and description, but not price -->
<!-- Student does not have edit priviledge -->
<% if current_user.role == "admin" || current_user.role == "instuctor" %>
  <%= link_to 'Edit', edit_course_path(@course) %> |
<% end %>
<%= link_to 'Back', courses_path %> |
<!-- If user has not already registered for the course then enable register link -->
<%= link_to_if Enrollment.where(user_id: current_user.id, course_id: @course.id).nil?,'Register Now', new_enrollment_path %>


<br>

<!-- <div id="placeholder-div1"></div>
<script>
  gapi.hangout.render('placeholder-div1', {
    'render': 'createhangout',
    'initial_apps': [{'app_id' : '184219133185', 'start_data' : 'dQw4w9WgXcQ', 'app_type' : 'ROOM_APP' }]
  });
</script>
</div> -->
